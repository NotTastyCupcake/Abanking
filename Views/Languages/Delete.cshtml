@model Work.Models.Language

@{
    ViewData["Title"] = "Удалиться";
}

<h1>Удаление</h1>

<h3>Вы точно хотите удалить этот язык?</h3>
<div>
    <h4>Язык</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NameLanguage)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NameLanguage)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdLanguage" />
        <input type="submit" value="Удалить" class="btn btn-danger" /> |
        <a asp-action="Index">Вернуться в список</a>
    </form>
</div>
