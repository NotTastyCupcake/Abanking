@model Work.Models.Language

@{
    ViewData["Title"] = "Изменить язык";
}

<h1>Изменить язык</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdLanguage" />
            <div class="form-group">
                <label asp-for="NameLanguage" class="control-label"></label>
                <input asp-for="NameLanguage" class="form-control" />
                <span asp-validation-for="NameLanguage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Изменить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Вернуться в список</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
